apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smallthinker-21b
  namespace: inference
spec:
  interval: 5m
  chart:
    spec:
      chart: ./kubernetes/infrastructure/inference/chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  values:
    name: smallthinker-21b
    backend: powerinfer

    source:
      huggingface: PowerInfer/SmallThinker-21BA3B-Instruct-GGUF
      contextSize: "32768"

    storage:
      sharedPvc: model-cache
      modelDir: smallthinker-21b
      modelFile: SmallThinker-21B-A3B-Instruct.Q4_0.powerinfer.gguf

    download:
      tokenSecret: hf-token

    resources:
      requests:
        memory: 12Gi
        cpu: "4"
      limits:
        memory: 20Gi

    scaling:
      cooldownPeriod: 300
      maxReplicas: 1

    nodeSelector:
      kubernetes.io/hostname: shadow

    tolerations:
      - key: amd.com/gpu
        operator: Exists
