---
# L2 Announcement Policy for Gateway LoadBalancer IPs
# Announces from all nodes on eno1 interface
apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: gateway-lb
spec:
  # Regex patterns covering all LAN interfaces: eno1, enp4s0, enp191s0, bond0, etc.
  interfaces:
    - eno1
    - enp.*
    - bond0
  nodeSelector:
    matchExpressions:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
  externalIPs: true
  loadBalancerIPs: true
---
# External Gateway for incoming HTTP/HTTPS traffic
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external-gateway
  namespace: kube-system
spec:
  gatewayClassName: envoy
  infrastructure:
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: external-gateway-proxy
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
    - name: http-domain1
      protocol: HTTP
      port: 80
      hostname: "*.${DOMAIN1}"
      allowedRoutes:
        namespaces:
          from: All
    - name: https-domain1
      protocol: HTTPS
      port: 443
      hostname: "*.${DOMAIN1}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-domain1-tls
            namespace: kube-system
    - name: https-domain2
      protocol: HTTPS
      port: 443
      hostname: "*.${DOMAIN2}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-domain2-tls
            namespace: kube-system
    - name: http-domain4
      protocol: HTTP
      port: 80
      hostname: "*.${DOMAIN4}"
      allowedRoutes:
        namespaces:
          from: All
    - name: https-domain4
      protocol: HTTPS
      port: 443
      hostname: "*.${DOMAIN4}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-domain4-tls
            namespace: kube-system
    - name: http-domain4-apex
      protocol: HTTP
      port: 80
      hostname: "heygolem.com"
      allowedRoutes:
        namespaces:
          from: All
    - name: https-domain4-apex
      protocol: HTTPS
      port: 443
      hostname: "heygolem.com"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-domain4-tls
            namespace: kube-system
    - name: ssh
      protocol: TCP
      port: 22
      allowedRoutes:
        namespaces:
          from: All
        kinds:
          - group: gateway.networking.k8s.io
            kind: TCPRoute
