apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: inference

resources:
  # Namespace must be first
  - namespace.yaml
  # Shared model cache PVC
  - model-cache.yaml
  # Model deployments (includes KittenTTS variants)
  - models/

# Common labels
commonLabels:
  app.kubernetes.io/part-of: inference
