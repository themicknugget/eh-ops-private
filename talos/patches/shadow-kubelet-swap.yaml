# Kubelet swap configuration for shadow node
# Apply with: talosctl patch machineconfig -n 192.168.1.56 --patch @shadow-kubelet-swap.yaml
machine:
  kubelet:
    extraConfig:
      memorySwap:
        swapBehavior: LimitedSwap
