---
apiVersion: v1
kind: Service
metadata:
  name: localai
  namespace: localai
  labels:
    app: localai
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: localai
