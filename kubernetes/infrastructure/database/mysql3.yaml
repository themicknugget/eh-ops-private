apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql3
  namespace: database
  labels:
    app: mysql3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql3
  template:
    metadata:
      labels:
        app: mysql3
    spec:
      nodeSelector:
        kubernetes.io/hostname: cloudbox3
      tolerations:
        - key: node-role.kubernetes.io/edge
          effect: NoSchedule
      containers:
        - name: socat
          image: alpine/socat:latest
          args:
            - TCP-LISTEN:3306,fork,reuseaddr,nodelay
            - TCP:10.0.2.198:3306,nodelay
          ports:
            - name: mysql
              containerPort: 3306
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          livenessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 2
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mysql3
  namespace: database
spec:
  selector:
    app: mysql3
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
