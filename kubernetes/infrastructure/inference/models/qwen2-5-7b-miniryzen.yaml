apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qwen2-5-7b-miniryzen
  namespace: inference
spec:
  interval: 5m
  chart:
    spec:
      chart: ./kubernetes/infrastructure/inference/chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  values:
    name: qwen2-5-7b-miniryzen
    backend: llamacpp-vulkan

    source:
      huggingface: bartowski/Qwen2.5-7B-Instruct-GGUF
      contextSize: "16384"

    storage:
      sharedPvc: model-cache-miniryzen
      modelDir: qwen2-5-7b
      modelFile: Qwen2.5-7B-Instruct-Q8_0.gguf

    download:
      include: "*Q8_0.gguf"
      tokenSecret: hf-token

    resources:
      requests:
        memory: 2Gi
        cpu: "4"
      limits:
        memory: 12Gi

    scaling:
      cooldownPeriod: 300
      maxReplicas: 1

    nodeSelector:
      kubernetes.io/hostname: miniryzen

    tolerations:
      - key: amd.com/gpu
        operator: Exists
